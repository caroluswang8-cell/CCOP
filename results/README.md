# Result artifacts

This directory contains the compact reports, configuration files, manuscript
tables, and figures for the affine and differential-vortex studies.

The large per-iteration JSON archives are generated by the corresponding
drivers and excluded from Git.  This avoids treating generated histories as
source code while preserving the exact commands, deterministic seeds, compact
tables, and plots needed to identify every reported run.

